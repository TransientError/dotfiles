source /home/kvwu/.local/lib/python3.8/site-packages/powerline/bindings/tmux/powerline.conf

set -g mouse on
setw -g mode-keys vi

bind-key j command-prompt -p "join pane from:" "join-pane -s '%%'"
bind-key k command-prompt -p "send pane to:" "join-pane -t '%%'"
